# rag
rag testing

Set virtual environment for python 10 and above
    PS D:\AI\OpenAI\microphone-gpt-project\src> py -3.13 -m venv env
    PS D:\AI\OpenAI\microphone-gpt-project\src> env\Scripts\activate

Install all dependencies
    pip install -r requirements.txt

pip install --upgrade chromadb langchain_chroma numpy

Git
    git config --global user.name "Eric"
    git config --global user.email "you@gmail.com"

1. Download and install Ollama and Deepseek 1.8
2. install: pip install -r requirements.txt
2. Download the sentense model to use it offline. Run the offline_model.py in setup folder.
